services:
  cypress-boiler:
    build: .
    container_name: cypress-boiler
    ports:
      - "3002:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./dev.db
      - PORT=3000
    restart: unless-stopped